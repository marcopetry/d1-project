import styled from 'styled-components'
import { theme } from 'utils/theme'

export const D1Hr = styled.hr`
  width: 100%;
  height: 1px;
  background-color: ${theme('colors.blueDark')};
  border: 1px solid ${theme('colors.blueDark')};
`
