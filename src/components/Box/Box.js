import styled from 'styled-components'
import { background, color, border, layout, space } from 'styled-system'

export const D1Box = styled.div`
  ${layout}
  ${space}
  ${border}
  ${background}
  ${color}
`
