import styled from 'styled-components'
import { color, space, typography } from 'styled-system'
import '@fonts/Gotham-Book.ttf'

export const D1Text = styled.span`
  ${typography}
  ${space}
  ${color}
`
